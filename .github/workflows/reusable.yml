name: re-usable
on: 
  workflow_call:
    inputs:
      person:
        description: Enter name
        required: true
        type: string
      
jobs:
  reusbale-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "${{ inputs.person}}in re-usbale job"


