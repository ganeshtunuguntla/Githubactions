name: Hello world
on: workflow_dispatch
jobs:
  Hello-job:  #name of job
    runs-on: ubuntu-latest
    steps:
      - name: hello step
        run: echo "Hello world"

  build:
    runs-on: ubuntu-latest
    needs: Hello-job
    steps:
      - name: build step
        
        run: |
          echo "in buils stage"
          echo "in line 2"
          
