# name: Hello world
# on: 
#   push:
#     branches:
#       - main
#       - feature
# jobs:
#   Hello-job:  #name of job
#     runs-on: ubuntu-latest
#     steps:
#       - name: hello step
#         if: github.ref == 'refs/heads/main'
#         run: echo "Hello world"
#          # echo "${{inputs.Environment}}"
#   outputs-job:
#     runs-on: ubuntu-latest
#     outputs:
#       output1: ${{steps.s1.outputs.version}}
#     steps:
#       - name: output step
#         id : s1
#         run : echo "version=1.0.0" >> $GITHUB_OUTPUT

#   build:
#     runs-on: ubuntu-latest
#     needs: outputs-job
#     steps:
#       - name: Connecting to env
#         env:
#             APP_ENV: "test"
#         run: echo "${{needs.outputs-job.outputs.output1}}"    
#       - uses: actions/checkout@v4
#       - run: cat index.html
          
